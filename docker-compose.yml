services:
  backend:
    build: ./Server
    ports:
      - "5000:5000"
    env_file:
      - ./Server/.env

  frontend:
    build: ./Client
    ports:
      - "5173:5173"
    env_file:
      - ./Client/.env
    depends_on:
      - backend